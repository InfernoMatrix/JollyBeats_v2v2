<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Song - Music Spot</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <div class="container">
    <header>
      <h1><a href="/">🎵 Music Spot</a></h1>
      <nav>
        <a href="/songs">Songs</a>
        <a href="/playlists">Playlists</a>
        <a href="/forum">Forum</a>
        <a href="/logout" class="btn-logout">Logout (<%= user.username %>)</a>
      </nav>
    </header>

    <main class="content">
      <div class="form-container">
        <h2>Edit Song</h2>
        
        <% if (error) { %>
          <div class="error-message"><%= error %></div>
        <% } %>

        <form action="/songs/edit/<%= song._id %>" method="POST" class="form">
          <div class="form-group">
            <label for="title">Title: *</label>
            <input type="text" id="title" name="title" value="<%= song.title %>" required>
          </div>

          <div class="form-group">
            <label for="artist">Artist: *</label>
            <input type="text" id="artist" name="artist" value="<%= song.artist %>" required>
          </div>

          <div class="form-group">
            <label for="album">Album:</label>
            <input type="text" id="album" name="album" value="<%= song.album %>">
          </div>

          <div class="form-group">
            <label for="genre">Genre:</label>
            <input type="text" id="genre" name="genre" value="<%= song.genre %>">
          </div>

          <div class="form-group">
            <label for="year">Year:</label>
            <input type="number" id="year" name="year" value="<%= song.year %>" min="1900" max="2026">
          </div>

          <div class="form-group">
            <label for="duration">Duration:</label>
            <input type="text" id="duration" name="duration" value="<%= song.duration %>">
          </div>

          <button type="submit" class="btn btn-primary">Update Song</button>
          <a href="/songs" class="btn btn-secondary">Cancel</a>
        </form>
      </div>
    </main>

    <footer>
      <p>&copy; 2025 Music Spot</p>
    </footer>
  </div>
</body>
</html>

